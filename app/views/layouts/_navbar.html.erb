<!-- Sticky Navbar Wrapper -->
<div class="sticky top-0 z-50 w-full bg-gradient-to-b from-white to-white shadow-sm sm:px-[116px]">

  <!-- 💻 Desktop Navbar -->
  <div class="hidden md:flex items-center justify-between px-6 py-4">
    <!-- Left: Logo + Location -->
    <div class="flex items-center gap-4">
      <a href="<%=root_path%>" class="">
      <div class="text-purple-600 text-2xl font-bold">
        <h3>User File Upload</h3>
      </div>
     </a>
      <div class="text-1xl font-bold">
        <h3>File upload</h3>
      </div>
    </div>

       <div class="flex items-center gap-6 text-sm text-gray-800">
        <%= button_to destroy_user_session_path, method: :delete, class: "flex items-center gap-2 cursor-pointer hover:text-purple-600 text-lg bg-transparent border-none p-0" do %>
          👤 <span>Logout</span>
        <% end %>
      </div>

  </div>


</div>


